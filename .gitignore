# Dataset — 55k images, ~1GB+. Never commit (link it in README instead).
archive (1)/
archive*.zip
*.png
!frontend/**/*.png

# Node
node_modules/
npm-debug.log*

# Secrets — never commit real SMS/gateway keys (only .env.example is tracked)
.env
backend/.env

# Trained model artifacts NOT meant to ship (raw Keras checkpoints, huge)
model/saved_model.keras
model/web_model/
model/*.h5
# NOTE: frontend/models/model.json + *.bin ARE committed on purpose — that's
# the actual ~9MB deployed TF.js model the app loads. Don't ignore it.

# Brainstorming source doc + scratch
DeepSeek Conversation.docx
~$*.docx
doc_text.txt
*.mermaid

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
